#version 330 core

in vec3 vertexColor;
in vec2 texCoordinates;
in vec3 worldNormal;

out vec4 color;

uniform sampler2D envmap;

void main()
{
    vec3 finalColor = texture(envmap, texCoordinates).rgb;

	finalColor = pow(finalColor, vec3(0.45));

    color = vec4(finalColor, 1.0f);
}
